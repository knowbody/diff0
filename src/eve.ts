/** Eve-specific collection and normalization, independently usable by custom hosts. */
export {
  EveCliAdapter,
  type EveEvalRunSummary,
  type SummaryContext,
  summaryToRunRecord,
} from "./adapters/eve.js";
